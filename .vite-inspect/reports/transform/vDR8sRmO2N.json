{
  "resolvedId": "/Users/rickysitorus/Downloads/Kerja/Pln Icon Plus/valiant-fe/src/services/format/global-format.ts",
  "transforms": [
    {
      "name": "__load__",
      "result": "export default class GlobalFormat {\n  formatEnergy(amount: any): string {\n    const formattedAmount = Number(amount).toFixed(2);\n    return formattedAmount.replace(/\\./g, ',');\n}\n  formatCurrency(amount: any): string {\n    const formattedAmount = Number(amount).toFixed(2);\n    const parts = formattedAmount.split('.');\n    parts[0] = parts[0].replace(/\\B(?=(\\d{3})+(?!\\d))/g, ',');\n    return parts.join('.');\n  }\n  formatRupiah(amount: any): string {\n    const formatter = new Intl.NumberFormat('en-Us', {\n      style: 'currency',\n      currency: 'IDR',\n      minimumFractionDigits: 2,\n      maximumFractionDigits: 2,\n    });\n      const formattedAmount = formatter.format(amount);\n      return formattedAmount.replace('Rp', '');\n  }\n  formatInputDecimalRupiah (inputValue: any) {\n    let inputValues = inputValue.replace(/[^\\d]/g, '');\n    if (inputValues.length > 2) {\n      const lastTwoDigits = inputValues.slice(-2);\n      const remainingDigits = inputValues.slice(0, -2);\n      inputValues = remainingDigits.replace(/(\\d)(?=(\\d{3})+(?!\\d))/g, '$1,') + '.' + lastTwoDigits;\n    }\n    return inputValues.replace(/(\\d)(?=(\\d{3})+(?!\\d))/g, '$1,');\n  };\n  formatInputDecimal (inputValue: any) {\n    let inputValues = inputValue.replace(/[^\\d]/g, '');\n    if (inputValues.length > 2) {\n      const integerPart = inputValues.slice(0, -2);\n      const decimalPart = inputValues.slice(-2);\n      inputValues = integerPart + ',' + decimalPart;\n    } else if (inputValues.length == 2){\n      const integerPart = inputValues.slice(0, -1);\n      const decimalPart = inputValues.slice(-1);\n      inputValues = integerPart + ',' + decimalPart;\n    }\n    return inputValues;\n  };\n  formatInputNumberOnly (inputValue: any) {\n    let inputValues = inputValue.replace(/[^\\d]/g, '');\n    return inputValues;\n  };\n\n}",
      "start": 1709022556146,
      "end": 1709022556146
    },
    {
      "name": "vite:esbuild",
      "result": "export default class GlobalFormat {\n  formatEnergy(amount) {\n    const formattedAmount = Number(amount).toFixed(2);\n    return formattedAmount.replace(/\\./g, \",\");\n  }\n  formatCurrency(amount) {\n    const formattedAmount = Number(amount).toFixed(2);\n    const parts = formattedAmount.split(\".\");\n    parts[0] = parts[0].replace(/\\B(?=(\\d{3})+(?!\\d))/g, \",\");\n    return parts.join(\".\");\n  }\n  formatRupiah(amount) {\n    const formatter = new Intl.NumberFormat(\"en-Us\", {\n      style: \"currency\",\n      currency: \"IDR\",\n      minimumFractionDigits: 2,\n      maximumFractionDigits: 2\n    });\n    const formattedAmount = formatter.format(amount);\n    return formattedAmount.replace(\"Rp\", \"\");\n  }\n  formatInputDecimalRupiah(inputValue) {\n    let inputValues = inputValue.replace(/[^\\d]/g, \"\");\n    if (inputValues.length > 2) {\n      const lastTwoDigits = inputValues.slice(-2);\n      const remainingDigits = inputValues.slice(0, -2);\n      inputValues = remainingDigits.replace(/(\\d)(?=(\\d{3})+(?!\\d))/g, \"$1,\") + \".\" + lastTwoDigits;\n    }\n    return inputValues.replace(/(\\d)(?=(\\d{3})+(?!\\d))/g, \"$1,\");\n  }\n  formatInputDecimal(inputValue) {\n    let inputValues = inputValue.replace(/[^\\d]/g, \"\");\n    if (inputValues.length > 2) {\n      const integerPart = inputValues.slice(0, -2);\n      const decimalPart = inputValues.slice(-2);\n      inputValues = integerPart + \",\" + decimalPart;\n    } else if (inputValues.length == 2) {\n      const integerPart = inputValues.slice(0, -1);\n      const decimalPart = inputValues.slice(-1);\n      inputValues = integerPart + \",\" + decimalPart;\n    }\n    return inputValues;\n  }\n  formatInputNumberOnly(inputValue) {\n    let inputValues = inputValue.replace(/[^\\d]/g, \"\");\n    return inputValues;\n  }\n}\n",
      "start": 1709022556146,
      "end": 1709022557069,
      "order": "normal"
    },
    {
      "name": "vite:import-analysis",
      "result": "export default class GlobalFormat {\n  formatEnergy(amount) {\n    const formattedAmount = Number(amount).toFixed(2);\n    return formattedAmount.replace(/\\./g, \",\");\n  }\n  formatCurrency(amount) {\n    const formattedAmount = Number(amount).toFixed(2);\n    const parts = formattedAmount.split(\".\");\n    parts[0] = parts[0].replace(/\\B(?=(\\d{3})+(?!\\d))/g, \",\");\n    return parts.join(\".\");\n  }\n  formatRupiah(amount) {\n    const formatter = new Intl.NumberFormat(\"en-Us\", {\n      style: \"currency\",\n      currency: \"IDR\",\n      minimumFractionDigits: 2,\n      maximumFractionDigits: 2\n    });\n    const formattedAmount = formatter.format(amount);\n    return formattedAmount.replace(\"Rp\", \"\");\n  }\n  formatInputDecimalRupiah(inputValue) {\n    let inputValues = inputValue.replace(/[^\\d]/g, \"\");\n    if (inputValues.length > 2) {\n      const lastTwoDigits = inputValues.slice(-2);\n      const remainingDigits = inputValues.slice(0, -2);\n      inputValues = remainingDigits.replace(/(\\d)(?=(\\d{3})+(?!\\d))/g, \"$1,\") + \".\" + lastTwoDigits;\n    }\n    return inputValues.replace(/(\\d)(?=(\\d{3})+(?!\\d))/g, \"$1,\");\n  }\n  formatInputDecimal(inputValue) {\n    let inputValues = inputValue.replace(/[^\\d]/g, \"\");\n    if (inputValues.length > 2) {\n      const integerPart = inputValues.slice(0, -2);\n      const decimalPart = inputValues.slice(-2);\n      inputValues = integerPart + \",\" + decimalPart;\n    } else if (inputValues.length == 2) {\n      const integerPart = inputValues.slice(0, -1);\n      const decimalPart = inputValues.slice(-1);\n      inputValues = integerPart + \",\" + decimalPart;\n    }\n    return inputValues;\n  }\n  formatInputNumberOnly(inputValue) {\n    let inputValues = inputValue.replace(/[^\\d]/g, \"\");\n    return inputValues;\n  }\n}\n",
      "start": 1709022557070,
      "end": 1709022557070,
      "order": "normal"
    }
  ]
}
